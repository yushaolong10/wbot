export interface AgentClient{request(path:string,init?:RequestInit):Promise<any>;eventsURL(sessionId:string):string}
export class HttpAgentClient implements AgentClient{constructor(private base:string,private token:string){}async request(path:string,init?:RequestInit){const r=await fetch(this.base+path,{...init,credentials:'include',headers:{'Content-Type':'application/json',...(this.token?{Authorization:`Bearer ${this.token}`}:{ }),...(init?.headers||{})}});const x=await r.json();if(!r.ok){const e=new Error(x.error||r.statusText) as Error&{status:number};e.status=r.status;throw e}return x}eventsURL(sessionId:string){return`${this.base}/api/v1/sessions/${sessionId}/events`}}
export function createAgentClient(){return new HttpAgentClient((localStorage.getItem('wbot_server_url')||'').replace(/\/$/,''),localStorage.getItem('wbot_token')||'')}
